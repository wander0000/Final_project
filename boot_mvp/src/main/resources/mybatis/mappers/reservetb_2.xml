<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.DAO.ReservetbDAO_2">   
    <insert id="insertReserve">
		insert into reservetb (uuid, reservenum, areano, theaterno, movieno, viewday, tmember, tprice)
				values (#{uuid}, #{reservenum}, #{areano}, #{theaterno}, #{movieno}, #{viewday}, #{tmember}, #{tprice})
    </insert>
</mapper>
